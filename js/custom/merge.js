let geomtry1,geometry2;

function merge (geometry1, geometry2){
    if (geometry1 !== geomtry2) {  // we are checking if the 2 geometries are not the same
            // check the position of the 1st geometry relative to the second geometry
            // get the vertices from one of the geomtries:
            const verticeGeom1 =  geometry1.vertices();
            console.log(verticeGeom1);

            const verticesGeom2 = geometry2.vertices();
            console.log(verticesGeom2);


            // get the vectors of each of the geometries

            console.log(geometries.giveVectors());
            // are they alining?
            geometry1.positionx 
    }
}


function giveVectors(geometry){

}